# Ignore build artifacts
build/

# Node modules (if ever added)
node_modules/

# Composer vendor directory
vendor/

# OS / editor files
.DS_Store
*.swp

# Additional build and development files
package-lock.json
.claude/
CLAUDE.md
.buildignore
build.sh

# WordPress documentation sync (contains application password)
docs/.docs-sync.json
